// firebase.js

const firebaseConfig = {
  apiKey: "ISI",
  authDomain: "ISI",
  databaseURL: "ISI",
  projectId: "ISI",
};

firebase.initializeApp(firebaseConfig);
const db = firebase.database();